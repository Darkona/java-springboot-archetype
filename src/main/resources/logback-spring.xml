<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Bring the default spring configuration for the console appender -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- LOKI push URL (override with -DLOKI_URL=... or env LOKI_URL) -->


    <!-- Local / docker profile: console + optional Loki when containerized -->
    <springProfile name="local">
        <property name="LOKI_URL" value="${LOKI_HOST:-http://localhost:9999}/loki/api/v1/push"/>
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>${CONSOLE_LOG_PATTERN:-%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger{36} - %msg%n}</pattern>
                <charset>UTF-8</charset>
            </encoder>
        </appender>

        <appender name="LOKI" class="com.github.loki4j.logback.Loki4jAppender">
            <labels>
                app = archetype
                env = local
                host=${HOST:-localhost}
            </labels>

            <message>
                <pattern>%-5level [%thread] %logger{20} - %msg%n</pattern>
            </message>

            <http>
                <url>${LOKI_URL}</url>
                <useProtobufApi>true</useProtobufApi>
                <requestTimeoutMs>10000</requestTimeoutMs>
                <maxRetries>3</maxRetries>
            </http>

            <batch>
                <maxItems>100</maxItems>
                <timeoutMs>5000</timeoutMs>
            </batch>

            <verbose>false</verbose>
        </appender>

        <!-- Async wrappers to avoid blocking application threads -->
        <appender name="ASYNC_CONSOLE" class="ch.qos.logback.classic.AsyncAppender">
            <appender-ref ref="CONSOLE"/>
            <queueSize>8192</queueSize>
            <discardingThreshold>0</discardingThreshold>
            <neverBlock>true</neverBlock>
        </appender>

        <appender name="ASYNC_LOKI" class="ch.qos.logback.classic.AsyncAppender">
            <appender-ref ref="LOKI"/>
            <queueSize>8192</queueSize>
            <discardingThreshold>0</discardingThreshold>
            <neverBlock>true</neverBlock>
        </appender>

        <root level="INFO">
            <appender-ref ref="ASYNC_CONSOLE"/>
            <appender-ref ref="ASYNC_LOKI"/>
        </root>
    </springProfile>

    <!-- Container / integration profile (explicit) -->
    <springProfile name="container, testInt">
        <property name="LOKI_URL" value="${LOKI_HOST:-http://alloy:9999}/loki/api/v1/push"/>
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>${CONSOLE_LOG_PATTERN:-%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger{36} - %msg%n}</pattern>
                <charset>UTF-8</charset>
            </encoder>
        </appender>

        <appender name="LOKI" class="com.github.loki4j.logback.Loki4jAppender">
            <labels>
                app = archetype
                env = container
                host=${HOST:-container}
            </labels>

            <message>
                <pattern>%-5level [%thread] %logger{20} - %msg%n</pattern>
            </message>

            <http>
                <url>${LOKI_URL}</url>
                <useProtobufApi>true</useProtobufApi>
                <requestTimeoutMs>10000</requestTimeoutMs>
                <maxRetries>3</maxRetries>
            </http>

            <batch>
                <maxItems>100</maxItems>
                <timeoutMs>5000</timeoutMs>
            </batch>

            <verbose>false</verbose>
        </appender>

        <appender name="ASYNC_CONSOLE" class="ch.qos.logback.classic.AsyncAppender">
            <appender-ref ref="CONSOLE"/>
            <queueSize>8192</queueSize>
            <discardingThreshold>0</discardingThreshold>
            <neverBlock>true</neverBlock>
        </appender>

        <appender name="ASYNC_LOKI" class="ch.qos.logback.classic.AsyncAppender">
            <appender-ref ref="LOKI"/>
            <queueSize>8192</queueSize>
            <discardingThreshold>0</discardingThreshold>
            <neverBlock>true</neverBlock>
        </appender>

        <root level="INFO">
            <appender-ref ref="ASYNC_CONSOLE"/>
            <appender-ref ref="ASYNC_LOKI"/>
        </root>
    </springProfile>

    <!-- Default / server profile: JSON output suitable for log aggregation -->
    <springProfile name="!local &amp; !docker">
        <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
                <charset>UTF-8</charset>
                <providers>
                    <timestamp>
                        <fieldName>timestamp</fieldName>
                        <pattern>yyyy-MM-dd'T'HH:mm:ss.SSSXXX</pattern>
                    </timestamp>
                    <logLevel>
                        <fieldName>level</fieldName>
                    </logLevel>
                    <loggerName>
                        <fieldName>logger</fieldName>
                    </loggerName>
                    <threadName>
                        <fieldName>thread</fieldName>
                    </threadName>
                    <message>
                        <fieldName>message</fieldName>
                    </message>
                    <mdc>
                        <fieldName>mdc</fieldName>
                    </mdc>
                </providers>
            </encoder>
        </appender>

        <appender name="ASYNC_JSON" class="ch.qos.logback.classic.AsyncAppender">
            <appender-ref ref="JSON"/>
            <queueSize>8192</queueSize>
            <discardingThreshold>0</discardingThreshold>
            <neverBlock>true</neverBlock>
        </appender>

        <root level="INFO">
            <appender-ref ref="ASYNC_JSON"/>
        </root>
    </springProfile>

</configuration>
